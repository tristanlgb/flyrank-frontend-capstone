# Notes for Reviewers

This submission intentionally keeps Round One weak so the comparison is meaningful. The first branch was produced from a single sentence with no project context, constraints, examples, or verification request. The second branch was created in a separate AI session using an explore-plan-code workflow and explicit requirements for validation, accessibility, edge cases, tests, type checking, and production build verification.

The branch diff is the main evidence for the assignment. Please compare form semantics, validation behavior, submission state, normalization of text input, test coverage, and the amount of manual review required. `WORKFLOW.md` documents the concrete mistakes found and the end-to-end effort for both rounds.
