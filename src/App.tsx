export function App() {
  return (
    <main className="shell">
      <section className="card" aria-labelledby="page-title">
        <p className="eyebrow">FlyRank capstone</p>
        <h1 id="page-title">Account settings</h1>
        <p className="intro">Settings form workflow drill.</p>
      </section>
    </main>
  );
}
